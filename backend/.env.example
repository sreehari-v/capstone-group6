PORT=5000
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d
EMAIL_VERIFY_EXPIRES_IN=1d
FRONTEND_URL=http://localhost:5173

# Optional: Google OAuth (if used)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:5000/api/auth/google/callback

# Optional SMTP settings for sending verification emails.
# If you don't provide SMTP settings, the server will use an Ethereal test account (dev only)
SMTP_HOST=smtp-relay.example.com
SMTP_PORT=587
SMTP_USER=<your_smtp_user>
SMTP_PASS=<your_smtp_password>
EMAIL_FROM="CareOn <no-reply@careon.local>"

# Optional: Gmail OAuth2 (preferred for long-lived service accounts)
# To use Gmail OAuth2, create OAuth client credentials in Google Cloud and obtain
# a refresh token for the sending account (or use the OAuth playground). Set
# the values below (do NOT commit secrets to git).
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=

# Alternative (simple, recommended for personal accounts): Gmail SMTP with
# an App Password. Create an App Password in your Google Account (requires 2FA)
# and set the SMTP_* values below. Make sure EMAIL_FROM matches SMTP_USER.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=princepr357@gmail.com
SMTP_PASS=gocs vszr fkyj rvds
EMAIL_FROM=princepr357@gmail.com

NODE_ENV=development
